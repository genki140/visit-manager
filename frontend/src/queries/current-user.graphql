# ログインユーザーの権限などの情報を取得
query getCurrentUser {
  currentUser {
    id
    name
    userOrganizations {
      id
      roles {
        name
        abilities {
          id
          name
        }
      }
      # organization {
      #   id
      #   name
      #   areas {
      #     id
      #     name
      #     description
      #     areaType {
      #       id
      #       name
      #     }
      #   }
      # }
    }
  }
}
